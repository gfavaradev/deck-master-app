rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Card images are public read, admin-only write
    match /yugioh/cards/{cardId} {
      allow read;
      allow write: if request.auth != null && request.auth.token.email == 'g.favara.dev@gmail.com';
    }
  }
}
